import './extension.js'
import './hot-reload-client.js'
import './settings.js'
import './utils.js'

console.log("Running where2search");


// document.addEventListener("DOMContentLoaded", () => {
//     const dropdown = document.getElementById("engine-selector") as HTMLSelectElement;

//     dropdown.addEventListener("change", async () => {
//         const selectedEngine = dropdown.value;
//         await saveDefaultEngine(selectedEngine);
//         alert(`Default search engine set to: ${selectedEngine}`);
//     });
// });
