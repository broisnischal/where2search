// export const saveDefaultEngine = async (engineKey: string) => {
//     await chrome.storage.sync.set({ defaultEngine: engineKey });
// };

// export const getDefaultEngine = async (): Promise<string | null> => {
//     const { defaultEngine } = await chrome.storage.sync.get("defaultEngine");
//     return defaultEngine || null;
// };
